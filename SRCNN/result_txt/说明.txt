说明：
800000是输入输出有12像素差的结果，Bicubic和SRCNN的差值基本符合要求，但是略差，B和S的值均偏小。
7000000还是上面的模型，但是训练的代数很多，B和S的差值已经很好，但是B和S的值均偏小。
7000000_new是将相差的12像素，不对输入进行正负6像素的裁剪，而是对输出进行pad，参数为‘edge’，显然，Bicubic的值变大，与paper相似甚至更高。
2700000是修改net模型之后的训练结果，显然，B和S的都增大，与paper一致甚至更高，且差值更大。